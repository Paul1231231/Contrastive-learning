"""Backbone encoder definition."""


class Encoder:
    """Encoder placeholder.

    Replace this with a real backbone (ResNet, ViT, etc.).
    """

    def __init__(self, name: str = "resnet18"):
        self.name = name

    def __call__(self, x):
        return x
